# TODO

- [ ] Publish workflow is stuck re-uploading unbumped `v0.2.0` to TestPyPI.
  Bump the version before the next publish attempt.
- [ ] `docs/README.md` duplicates the root `README.md`'s content — check
  whether one should just link to the other instead of maintaining two copies.
